name: mobcar
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.1.5 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: 
  flutter_modular: 
  image_picker:
  equatable: 
  hive: 
  hive_flutter: 
  http: 
  smac_dart:
    git:
      url: https://github.com/Jhoe-Ohaze/smac_dart.git
      ref: main

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  flutter_launcher_icons: ^0.9.3

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "lib/assets/icon.png"  


flutter:
  uses-material-design: true